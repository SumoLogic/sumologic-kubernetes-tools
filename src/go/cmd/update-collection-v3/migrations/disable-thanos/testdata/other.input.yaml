kube-prometheus-stack:
  prometheus:
    prometheusSpec:
      scrapeInterval: 1m
      thanos:
        image: custom.registry.com/custom-repository/thanos:v0.25.2
sumologic:
  accessId: xxx
  accessKey: yyy
