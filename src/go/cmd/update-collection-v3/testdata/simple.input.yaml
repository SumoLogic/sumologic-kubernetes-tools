fluentd:
  events:
    enabled: false
kube-prometheus-stack:
  prometheus:
    prometheusSpec:
      thanos:
        resources:
          limits:
            cpu: 20m
otelevents:
  config:
    override:
      key: value
metadata:
  logs:
    config:
      keylogs: valuelogs
  metrics:
    config:
      keymetrics: valuemetrics
sumologic:
  accessId: xxx
  accessKey: yyy
