fluentd:
  events:
    enabled: false
kube-prometheus-stack:
  prometheus:
    prometheusSpec:
      thanos:
        resources:
          limits:
            cpu: 20m
otelevents:
  config:
    override:
      key: value
metadata:
  logs:
    config:
      key: value
sumologic:
  accessId: xxx
  accessKey: yyy
