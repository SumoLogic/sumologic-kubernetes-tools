sumologic:
  accessId: xxx
  accessKey: yyy
fluentd:
  events:
    enabled: false
  logs:
    autoscaling:
      enabled: true
  metrics:
    autoscaling:
      enabled: true
kube-prometheus-stack:
  prometheus:
    prometheusSpec:
      thanos:
        resources:
          limits:
            cpu: 20m
metadata:
  logs:
    config:
      keylogs: valuelogs
  metrics:
    config:
      keymetrics: valuemetrics
otelevents:
  config:
    override:
      key: value
otellogs:
  config:
    override:
      key: value
