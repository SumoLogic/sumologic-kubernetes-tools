metadata:
  logs:
    config:
      merge:
        keylogs: valuelogs
  metrics:
    config:
      merge:
        keymetrics: valuemetrics
otelevents:
  config:
    merge:
      key: value
sumologic:
  accessId: xxx
  accessKey: yyy
  events:
    enabled: false
