sumologic:
  accessId: xxx
  accessKey: yyy
  events:
    enabled: false
  logs:
    container:
      format: text
fluentd:
  logs:
    autoscaling:
      enabled: true
  metrics:
    autoscaling:
      enabled: true
metadata:
  logs:
    config:
      merge:
        keylogs: valuelogs
    statefulset:
      stsLogsKey: stsLogsValue
    autoscaling:
      enabled: true
  metrics:
    config:
      merge:
        keymetrics: valuemetrics
    statefulset:
      stsMetricsKey: stsMetricsValue
    autoscaling:
      enabled: true
otelevents:
  config:
    merge:
      key: value
otellogs:
  config:
    merge:
      key: value
