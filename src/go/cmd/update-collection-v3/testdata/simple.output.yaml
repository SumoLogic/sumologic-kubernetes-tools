sumologic:
  accessId: xxx
  accessKey: yyy
  events:
    enabled: false
fluentd:
  logs:
    autoscaling:
      enabled: true
  metrics:
    autoscaling:
      enabled: true
metadata:
  logs:
    config:
      merge:
        keylogs: valuelogs
    autoscaling:
      enabled: true
  metrics:
    config:
      merge:
        keymetrics: valuemetrics
    autoscaling:
      enabled: true
otelevents:
  config:
    merge:
      key: value
otellogs:
  config:
    merge:
      key: value
