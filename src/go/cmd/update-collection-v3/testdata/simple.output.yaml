fluentd:
  logs:
    autoscaling:
      enabled: true
  metrics:
    autoscaling:
      enabled: true
metadata:
  logs:
    autoscaling:
      enabled: true
    config:
      merge:
        keylogs: valuelogs
  metrics:
    autoscaling:
      enabled: true
    config:
      merge:
        keymetrics: valuemetrics
otelevents:
  config:
    merge:
      key: value
otellogs:
  config:
    merge:
      key: value
sumologic:
  accessId: xxx
  accessKey: yyy
  events:
    enabled: false
